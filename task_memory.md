# Task Memory

此文件按完成时间顺序记录任务交接内容。记录时间、内容、验证和后续事项，暂不记录操作者。

## 2026-07-21 12:20｜初始化项目仓库与协作文档

- 状态：已完成
- 目标：初始化空仓库，建立前后端分离架构和持续维护的项目文档。
- 完成内容：建立 React + TypeScript + Vite 前端、FastAPI 后端、健康检查接口、自动化测试，以及文档和 TODO 维护规范。
- 关键决策：前后端独立维护；开发环境由 Vite 将 `/api` 代理到后端；接口统一使用 `/api/v1` 前缀。
- 验证结果：前端 lint 与生产构建通过；后端 1 个测试及 Ruff 检查通过；`git diff --check` 通过。
- 后续事项：明确首个业务模块，之后设计数据模型、认证授权和持续集成方案。

## 2026-07-21 12:24｜调整仓库目录与独立文档结构

- 状态：已完成
- 目标：将仓库业务目录限制为 `FrontEnd`、`BackEnd`、`Docs` 和 `Test`，将 memory、TODO、README、architecture 改为独立文档。
- 完成内容：统一目录名称和大小写；将测试迁移到根目录 `Test/`；将架构文档迁移到根目录；将历史任务记录合并到单一 `task_memory.md`；修正全部路径引用与测试配置。
- 关键决策：后续所有任务记录都追加到本文件，不再为任务创建目录或独立记录文件。
- 验证结果：前端 lint 与生产构建通过；后端 1 个测试及 Ruff 检查通过；根目录检查确认业务目录仅有指定的四个；`git diff --check` 通过。
- 后续事项：按新的四目录和四文档结构继续开发。

## 2026-07-21 16:13｜优化架构师-Codex 协作协议

- 状态：已完成
- 目标：解决桥接任务目录设计与现有仓库约束的冲突，并减少多个进度文档之间的重复维护。
- 完成内容：将任务 000 调整为只使用 `FrontEnd`、`BackEnd`、`Docs`、`Test` 四个顶层业务目录，AI/ML 源码、配置、实验、notebook 和脚本统一放入 `BackEnd/`；新增文档职责表、同步时机和冲突优先级；同步更新工作流、README 与 TODO。
- 关键决策：桥接文档负责当前任务与待决策事项；`TODO.md` 负责简明路线图；`task_memory.md` 只追加历史；`ARCHITECTURE.md` 只记录已确认且生效的架构；尚未正式确认的 proposal 内容不自动成为架构决策。
- 验证结果：检查根目录业务文件夹仍仅有四个；全文检查旧目录树和“单一真相来源”表述已被替换；`git diff --check` 通过。
- 后续事项：项目方向和仿真方案等待正式确认，不提前执行相关架构决策。

## 2026-07-22 09:29｜初始化 CI/CD 流水线与 push 检查规范

- 状态：已完成
- 目标：在业务开发开始前建立 push 和 pull request 的自动质量门禁。
- 完成内容：新增 GitHub Actions 工作流；前端执行锁定依赖安装、ESLint 和生产构建，后端执行开发依赖安装、Ruff 和 pytest；新增根目录 `./start.sh`，按需初始化依赖、同时启动前后端并统一处理退出；补充本地检查、pull request 合并与 `main` 分支保护规范。
- 关键决策：所有分支的 push 和 pull request 均触发检查；两个检查并行运行并取消同分支的过期任务；部署目标和环境未确定前仅建立 CI，不配置部署凭据或自动发布。
- 验证结果：前端 lint 与生产构建通过；后端 Ruff lint/格式检查和 pytest 通过；一键启动脚本语法与前后端健康状态验证通过；工作流 YAML 与文档完成静态检查；`git diff --check` 通过。
- 后续事项：工作流首次在 GitHub 运行成功后，将 `Frontend checks`、`Backend checks` 和 `Project checks` 配置为 `main` 的必需状态检查；确定部署平台后新增 CD job。

## 2026-07-22 23:00｜确认项目方向与技术方案

- 状态：已完成
- 目标：通过三轮深入问题讨论，明确项目最终方向、技术选型和实施路径。
- 完成内容：
  - 确认项目核心产物：完整端到端平台（仿真+训练+前端），不包含真实部署
  - 确认技术栈：Transformer（车辆+事件token分离）+ PPO（批量决策，Multi-discrete动作空间）
  - 确认场景规模路径：B级（30-50车，简单路网）→ C级（100车，城市路网）→ D级（数百车，真实地图，可选）
  - 确认网络模型：3GPP TR 38.901信道模型为主，可选探索NS-3集成
  - 确认前端技术：React + Leaflet + D3.js，后期可加Three.js 3D可视化
  - 确认通信协议：WebSocket实时双向通信
  - 确认训练数据规模：50场景×10次（约500轮仿真）
  - 确认实验设计：基础性能表 + 消融实验 + 泛化性分析
  - 确认演示策略：预设场景 + 实时交互 + 并排对比视图 + 3D可视化（后期）
- 关键决策：
  - 项目分5个里程碑（M0-M5），时间跨度16周
  - 文献检索确认创新性：现有研究未覆盖完整技术链（Transformer风险建模 + RL联合调度）
  - 演示核心："对比场景动画 + 实时交互 + 3D视角切换"构成三大wow moment
- 验证结果：
  - 更新 `ARCHITECTURE.md`：清理重复内容，添加模块状态标注，更新启动方式为 `./start.sh`
  - 更新 `TODO.md`：按M0-M1里程碑组织待办事项
  - 更新 `ARCHITECT_CODEX_BRIDGE.md`：新增5个详细任务（任务000-005），包含验收条件和执行权限边界
  - 文档结构规划：确定需创建 `TECHNICAL_SPECIFICATION.md`、`IMPLEMENTATION_ROADMAP.md`、`DEMO_GUIDE.md`
- 后续事项：
  - 执行任务000：完善后端目录结构
  - 执行任务001：配置Python环境和依赖
  - 执行任务003：SUMO安装验证
  - 执行任务004：WebSocket基础通信
  - 执行任务005：创建技术文档三件套

## 2026-07-22 23:30｜完成M0阶段所有任务

- 状态：已完成
- 目标：建立开发环境，完成项目骨架，验证核心工具链。
- 完成内容：
  - **任务000**：后端目录结构完善（src/models, environment, training, evaluation, deployment + configs + experiments + notebooks + scripts）
  - **任务001**：Python环境配置（requirements.txt + environment.yml，包含PyTorch, Stable-Baselines3, Gymnasium, SUMO等）
  - **任务003**：SUMO安装验证（verify_sumo.py脚本 + 集成测试，验证SUMO 1.27.1可用）
  - **任务004**：WebSocket基础通信（后端/ws/simulation端点 + 前端useWebSocket hook，测试消息推送正常）
  - **任务005**：创建技术文档（TECHNICAL_SPECIFICATION.md + IMPLEMENTATION_ROADMAP.md + DEMO_GUIDE.md）
- 关键决策：
  - 目录结构遵循四顶层目录约束（FrontEnd/BackEnd/Docs/Test）
  - Python依赖版本：PyTorch 2.13.0, Gymnasium 1.3.0, Stable-Baselines3 2.9.0
  - SUMO通过traci和sumolib集成
  - WebSocket通信验证通过（连接、断开、重连机制正常）
- 验证结果：
  - 所有目录结构创建并添加__init__.py
  - .gitignore已更新（experiments/, *.pyc, .ipynb_checkpoints/, *.pt等）
  - README.md完整更新（项目架构、快速开始、安装指南、技术栈）
  - 依赖安装测试通过（Python 3.12.13环境）
  - SUMO验证脚本运行成功
  - WebSocket前后端通信测试通过
  - 三份技术文档已创建
- 后续事项：
  - 进入M1阶段：简单场景原型（Week 3-5）
  - 执行任务006：SUMO高速公路场景生成
  - 执行任务007：Transformer模型实现
  - 执行任务008：简化网络抽象层

## 2026-07-22｜完成M0交接任务000、001、003、004、005

- 状态：已完成
- 目标：按架构师桥接文档完成目录、Python依赖、SUMO验证、WebSocket基础通信和技术文档。
- 完成内容：补齐AI/ML与测试目录；建立Python 3.12环境和Conda配置；安装并验证PyTorch、Gymnasium、Stable-Baselines3与SUMO；实现 `/ws/simulation`、前端接收显示及2秒自动重连；将三份技术草稿归档到 `Docs/` 指定路径。
- 关键修正：正式评分文件规定presentation为20分钟，因此演示指南使用20分钟；同一场景配置的随机种子保持在同一数据集合；未验证实验数字不得作为已有成果。
- 验证结果：后端Ruff和格式检查通过，完整pytest为4 passed；前端ESLint和生产构建通过；浏览器验证持续收信、断开状态和自动重连；SUMO最小场景运行1秒并通过集成测试；Python 3.12核心依赖导入成功且 `pip check` 无冲突。
- 遗留说明：本机缺少 `tree` 命令，两次Homebrew安装因外部TLS失败；目录已通过等价的 `find` 检查。现有Markdown草稿包含行尾空格，因此 `git diff --check` 仍会报告这些原有格式问题，本轮未为此改写架构内容。
- 后续事项：进入M1简单场景原型；任何公共接口、算法、数据格式、基线或指标变更继续按桥接文档请求架构师批准。

## 2026-07-23｜完成M1任务006、007、008

- 状态：已完成
- 目标：按架构师桥接文档完成高速场景生成、Transformer环境编码器和简化网络抽象层。
- 完成内容：生成可由SUMO加载的3车道5公里高速场景、30-50辆车轨迹与急刹事件；实现4层8头、256维的车辆/事件分离Transformer编码器；实现时延、距离丢包与优先级带宽分配模型；补齐单元测试及CI所需开发依赖。
- 关键决策：Transformer输出保留batch维度；网络模型以结构化结果返回时延、丢包率和分配带宽；相对输出路径统一解析到 `BackEnd/`，并生成完整SUMO运行包。
- 验证结果：Ruff lint与格式检查通过；后端完整测试16项通过；默认场景由SUMO/TraCI实际加载并生成50辆车、2个事件；前端ESLint与生产构建通过。
- 后续事项：M1剩余Gymnasium环境、PPO基础版、基线方法和前端可视化按新的架构师任务继续执行。

## 2026-07-23｜完成M1任务009至013

- 状态：已完成
- 目标：完成离线强化学习闭环、基线评估能力以及不依赖真实后端状态的前端可视化原型。
- 完成内容：实现固定50车/2事件padding的10步Gymnasium离线环境、简化奖励、Transformer特征提取PPO、10-episode训练脚本、全量广播与300米距离基线、三方法评估器；前端以固定5车、1事件、3条消息mock数据实现Leaflet三车道地图、传播动画和指标卡。
- 关键决策：关键接收者统一定义为事件300米内车辆；PPO动作固定为50个接收标记加优先级和带宽档位；训练前后使用固定验证评估并保存非退化最佳检查点；本轮不定义 `state_update`，现有WebSocket test消息保持兼容。
- 验证结果：10个episode训练完成，验证平均reward由0.241提升至0.642，模型和TensorBoard日志生成；后端完整测试27项通过，Ruff通过；前端ESLint、生产构建及浏览器验收通过，确认3条车道、5辆车、1个事件、3条1秒传播动画和固定指标正常显示。
- 后续事项：由后续任务统一定义完整WebSocket状态协议并接入真实Environment数据；继续实现紧急度基线、完整系统集成和M2场景。

## 2026-07-23｜完成任务014至018及M1端到端验收

- 状态：已完成
- 目标：完成真实PPO仿真到前端的完整M1链路，并准备M2的紧急度基线、100车城市场景和3GPP网络模型。
- 完成内容：新增可控制的仿真WebSocket和只读环境快照；前端移除mock并显示真实车辆、事件、传输、指标与PPO决策；评估器扩展为AI加三种基线；生成三信号交叉口、100辆车和三类事件的城市SUMO场景；网络模型新增可配置3GPP路径损耗、SINR丢包和排队时延，同时保留simple模式。
- 关键决策：连接后自动播放，重置后暂停；实时指标按当前仿真步计算；紧急度基线使用结构化资源分配；城市训练使用独立配置；默认网络模式保持simple，3GPP物理参数可由YAML覆盖。
- 验证结果：后端完整测试52项通过，Ruff lint与格式检查通过；前端lint与生产构建通过；真实浏览器完成10步M1演示和控制验证；真实SUMO生成并加载100辆车、3个信号交叉口及急刹、障碍物、碰撞预警三类事件；城市100车环境完成PPO预测冒烟。
- 后续事项：可使用 `configs/training_urban.yaml` 启动M2训练，并根据实验设计将network模式从simple切换为3gpp开展对比。

## 2026-07-23｜完成M2任务019至023完整可视化

- 状态：已完成
- 目标：展示Transformer注意力与PPO决策依据，增加实时指标曲线和AI/基线同步对比，并改善车辆动画流畅度。
- 完成内容：后端从PPO单次推理提取最终Transformer层事件到车辆的相对注意力，扩展候选车辆、选中车辆和选择原因协议；前端新增注意力热区与连线、可折叠决策侧栏、D3资源和三联时序图；新增双环境比较WebSocket及全广播、距离、紧急度三种分屏基线；车辆使用150ms平滑位置与最短航向角插值。
- 关键决策：注意力按最终层8头均值并逐事件最大值归一化，取全局Top 10，权重大于0.6才连线，界面明确标为相对权重而非因果解释；比较环境使用相同场景和随机种子42，仅在AI与基线时间戳匹配时原子更新；单模型协议保持向后兼容。
- 验证结果：后端63项pytest、Ruff lint和格式检查通过；前端lint、生产构建及5项状态配对/插值测试通过；真实浏览器完成注意力、候选车辆、资源图、三联曲线、分屏控制和390px响应式验收；真实VehicleLayer临时基准测得50辆约59.8fps、100辆约56.4fps。
- 后续事项：可在M2城市100车模型训练完成后，将默认演示资源切换为城市配置并记录正式实验指标；注意力可视化继续只作为模型关注线索解释。

## 2026-07-23｜完成M2正式实验流水线实现

- 状态：流水线实现已完成，正式实验运行待团队GPU执行
- 目标：按任务024至029建立可复现、可恢复且不发生测试集泄漏的训练与论文结果生成流程。
- 完成内容：扩展五组件奖励和统一100车/3事件模型空间；建立50/7/10训练、验证、测试场景矩阵；实现高速与城市批量PPO、奖励权重验证选择、独立验证冠军选择、AI与三基线固定对比、Transformer-only与RL-only干净消融、双向跨域泛化，以及带正式完整性门禁的LaTeX/PNG/Markdown结果生成。
- 关键决策：安全时间窗固定100 ms；早停耐心为20个训练episode；Transformer-only使用监督接收者排序并只在验证集选择Top-K；RL-only使用距离、速度和相对位置手工特征PPO；bootstrap为10000次，显著性使用配对Wilcoxon并做Holm校正。
- 验证结果：后端完整pytest 73项通过；全仓Ruff lint与格式检查通过；真实Mac smoke完成1个高速配置训练、验证冠军选择及模型/日志/汇总落盘；结果生成smoke验证会明确标记且不能作为正式结论。
- 后续事项：团队在GPU和外部实验存储上完成任务024、025正式训练，再依次运行任务026至029；正式结果产生前不填写性能或创新性结论。

## 2026-07-24｜完成任务030至034可执行部分

- 状态：算法与演示框架已完成，正式GPU实验和缺失演示模型待执行
- 目标：完善奖励函数与输入特征，建立Transformer架构搜索能力，并将前端升级为适合presentation的专业演示界面。
- 完成内容：
  - **任务030**：实现决策、排队、传输三段时延，增加事件严重度加权、受影响车辆覆盖公平性以及六项完整奖励；建立7场景、3125组权重离线搜索脚本和配置。
  - **任务031**：增加车辆间、车辆与事件间相对特征，加速度、TTC、换道频率、5步历史轨迹、速度变化及车道前后车辆上下文；通过 `basic/enhanced` 开关保持旧模型兼容。
  - **任务032**：解除Transformer固定4层、8头、256维限制，兼容12头非整除维度；实现距离邻接Graph Transformer及基于Optuna、SQLite可恢复的架构搜索框架。
  - **任务033**：建立深色专业主题、8px间距体系、统一Lucide图标、加载骨架、Toast、连接反馈、响应式布局和减少动画支持。
  - **任务034**：实现演示模式、三个安全预设场景、播放/暂停/切换/循环、1x/2x/5x速度、关键时刻动效及基于真实WebSocket状态的自动解说；缺少正式模型的场景明确禁用。
- 关键决策：本轮只运行CPU smoke和真实高速场景演示；离线权重排名、架构延迟基准不得作为正式性能结论；城市与多事件演示不使用mock或虚假模型替代；旧PPO模型默认继续使用基础5维特征。
- 验证结果：权重脚本完成3125组CPU smoke；架构脚本、Graph Transformer及12头配置运行成功；后端完整pytest 81项通过，Ruff lint与格式检查通过；前端ESLint和生产构建通过；真实浏览器验证1920×1080与1280×720无横向溢出，高速AI/全广播WebSocket对比完成10步且控制台无错误。
- 后续事项：GPU可用后完成奖励候选正式复训、增强特征性能对比和54组架构搜索，再执行任务024至029正式实验；补齐城市与多事件冠军模型后完成任务034三个真实场景的连续自动演示验收。

## 2026-07-24｜修复Ruff版本漂移导致的CI失败

- 状态：已完成
- 根因：后端开发依赖允许安装任意低于1.0的Ruff版本，GitHub Actions因此安装0.16.0，而本地验证使用0.15.22；规则行为变化导致原本通过的代码在CI集中报出82项问题。
- 修复内容：在`pyproject.toml`、`requirements.txt`和`environment.yml`中统一锁定Ruff 0.15.22，并在Ruff配置中声明必需版本，确保本地与CI使用同一检查器。
- 验证结果：按GitHub Actions顺序重新执行Ruff lint、格式检查和完整后端测试，均通过。

## 2026-07-24｜完成M3非GPU任务035至037

- 状态：已完成；任务024至032所需正式GPU训练与实验仍保持进行中。
- 目标：在不依赖GPU训练结果的前提下，增强最终演示的三维视角、真实地图观感和现场场景编辑能力。
- 完成内容：实现React Three Fiber鸟瞰场景、道路/车辆/事件/消息三维渲染和2D/3D同步切换；接入OpenStreetMap街道图与Esri免密钥卫星图，完成SUMO局部坐标投影和瓦片失败回退；实现1–100辆车、0–5个事件的编辑器、三种极端预设和版本化JSON导入导出；新增临时场景预览API并复用现有WebSocket运行链路。
- 关键边界：当前真实PPO模型最多运行50辆车、2个事件；51–100辆车或3–5个事件只允许编辑、渲染和导出，界面明确禁用AI运行，避免把未支持规模误展示为真实推理结果。临时场景写入系统临时目录并自动清理，不进入正式实验数据。
- 验证结果：前端lint、单元测试和生产构建通过；后端Ruff、格式检查及新增API/WebSocket集成测试通过；浏览器验证40辆车/5事件3D渲染、100辆车能力门禁、街道/卫星切换，以及2辆车自定义场景完成5秒真实WebSocket仿真。
- 后续事项：GPU可用后继续任务024至032正式训练和实验；用目标演示硬件补录50车3D帧率，随后再根据正式实验结论优化演示叙事与视觉细节。

## 2026-07-24｜完成任务038至043动画叙事实装

- 状态：代码、自动化测试和构建已完成；目标Mac Chrome的Performance录制与60秒端到端视觉验收待执行。
- 目标：把由WebSocket状态跳变驱动的简陋演示升级为连续、可解释且适合presentation的2D/3D视觉叙事，同时坚持只展示真实后端状态。
- 完成内容：新增独立动画引擎、非线性物理插值、断线滑行和全局慢动作；升级2D/3D车辆及状态光效；以有界粒子池实现消息传播和AI/基线叠加；增加冲击波、雷达扫描、候选高亮与300米风险区；实现尊重用户手动操作的智能镜头；按0–60秒八阶段重构演示控制和真实数据解说。
- 接口补充：演示场景接口返回场景文件中的真实事件元数据；AI决策可选返回`inference_time_ms`，基线不伪造该字段；现有WebSocket测试消息和`state_update`必填结构保持兼容。
- 关键决策：后端演示固定0.25倍运行，叙事使用独立墙钟；比较阶段先叠加AI亮色与基线灰色粒子，再进入左右分屏；高频对象由动画引擎直接驱动，React只管理低频界面状态；减少动态效果偏好会关闭非必要动画。
- 验证结果：前端ESLint通过、14项Node测试通过、生产构建通过；后端Ruff lint/格式检查和87项pytest通过。浏览器控制连接不可用，因此未声称已完成50车/10并发消息60fps及完整60秒演示的Performance验收。
- 后续事项：在目标Mac Chrome 1920×1080录制Performance并走完演示；若帧率不足，优先降低粒子密度而不改变真实数据和叙事含义。GPU可用后继续任务024至032正式实验。

## 2026-07-25｜完成高价值依赖解耦

- 状态：已完成，不改变REST、WebSocket、算法、指标或用户可见行为。
- 目标：处理结构审查中发现的跨层反向依赖、模块级全局单例、入口职责过多和单/对比WebSocket重复逻辑，同时避免细拆稳定的训练主链。
- 完成内容：前端以Runtime Context注入动画和镜头实例，提取Simulation Session与Demo Presentation Hook，删除ComparisonView和AttentionViz转发目录；后端抽离不可变仿真快照类型，以PlaybackController和统一AI step复用控制/推理流程，并将实时基线动作迁入核心评估层。
- 依赖规则：`BackEnd/src`不得导入`BackEnd/app`；实验与API共用核心基线实现；前端组件通过Runtime和Hook访问动画、镜头与连接状态，不直接依赖模块级单例。`V2XEnv`继续保持场景、特征、奖励和step的完整环境职责，本轮只抽离共享类型。
- 兼容性：所有HTTP/WebSocket路径和消息结构保持不变；`v2x_env`继续重新导出快照类型，`app.comparison`继续提供兼容导出。
- 验证结果：前端ESLint、16项Node测试和生产构建通过；后端Ruff检查和95项pytest通过，其中新增依赖边界、播放状态、速度范围、Runtime隔离及端点构造测试。
- 后续事项：继续完成GPU正式实验和目标Chrome动画性能验收；不因本轮结构重构扩大算法或视觉范围。
